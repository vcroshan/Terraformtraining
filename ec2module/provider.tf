/*provider "aws" {
  region     = var.region
  access_key = var.accesskey
  secret_key = var.secretkey
}

provider "aws" {
  region     = var.region
  access_key = var.accesskey
  secret_key = var.secretkey
  alias      = "west"
}*/